@model IEnumerable<ePro.Models.Compliance>

@{
    ViewBag.Title = "Compliance Matrix";
}

<h2>Compliance Matrix</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ComplianceForm.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ComplianceItem.ItemName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ComplianceForm.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ComplianceItem.ItemName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Order)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ComplinanceID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ComplinanceID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ComplinanceID })
        </td>
    </tr>
}

</table>
